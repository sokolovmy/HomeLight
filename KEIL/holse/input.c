#include "input.h"
#include "main.h"
#include "cmsis_os.h"


//bool 

void inputThread(void * arg){
	while(1){
	//	HAL_SPI
		//osDelay(10);
	};
};

void inputInit(void){
	//osThreadNew(inputThread, NULL, NULL);
}

//int inputInit() {
	//HAL_GPIO_WritePin(IN_INHB_GPIO_Port, IN_INHB_Pin, GPIO_PIN_SET);

//}